<?php include_once(__DIR__ . '/../Partials/Page/Head.phtml'); ?>

<body>
    <div class="container">
        <form action="" method="post">
            <div class="form-group">
                <label for="language">Select a target language</label>
                <select class="form-control" name="language" id="language">
                <?php foreach ($languages as $languageOption) : ?>
                    <?php if (isset($language) && $languageOption === $language) : ?>
                <option selected="selected"><?= $languageOption ?></option>
                    <?php elseif ($languageOption === 'English') : ?>
                <option selected="selected"><?= $languageOption ?></option>
                    <?php else : ?>
                <option><?= $languageOption ?></option>
                    <?php endif ?>
                <?php endforeach ?>
                </select>
                <?php if (isset($languageFalse) && $languageFalse) : ?>
                <div class="alert alert-danger">Select a language from the list</div>
                <?php endif ?>
            </div>
            <div class="form-group">
                <label for="word-1">Word</label>
                <input class="form-control" type="text" name="word-1" id="word-1" placeholder="vorto">
                <?php if (isset($wordEmpty) && $wordEmpty) : ?>
                <div class="alert alert-danger">Word is empty</div>
                <?php endif ?>
            </div>
            <button type="submit" class="btn btn-primary">Build my deck</button>
        </form>
    </div>
</body>
