<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?= $title ?></title>
</head>
<body>
    <form action="" method="post">
        <div>
            <label for="language">Select a target language: </label>
            <select name="language" id="language">
                <?php
                    // $languages = langues disponibles
                    // $language = langue sélectionnée par l’utilisateur, récupérée depuis $_POST['language']
                ?>
                <?php foreach ($languages as $languageOption) : ?>
                    <?php if (isset($language) && $languageOption === $language) : ?>
                        <option selected="selected"><?= $languageOption ?></option>
                    <?php elseif ($languageOption === 'English') : ?>
                <option selected="selected"><?= $languageOption ?></option>
                    <?php else : ?>
                <option><?= $languageOption ?></option>
                    <?php endif ?>
                <?php endforeach ?>
            </select>
        </div>
        <?php if (isset($languageFalse) && $languageFalse) : ?>
            <p>Select a language from the list</p>
        <?php endif ?>
        <div>
            <label for="word-1">Word: </label>
            <input type="text" name="word-1" id="word-1">
        </div>
        <?php if (isset($wordEmpty) && $wordEmpty) : ?>
            <p>Word is empty</p>
        <?php endif ?>
        <button>Build my deck</button>
    </form>
</body>
